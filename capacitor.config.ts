import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.neureuther',
  appName: 'Neureuther',
  webDir: 'dist'
};

export default config;
